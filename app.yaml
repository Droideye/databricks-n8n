command: ["python", "app.py"]
env:
  - name: PORT
    value: "8000"
  - name: N8N_PORT
    value: "8000"
  - name: N8N_LISTEN_ADDRESS
    value: "0.0.0.0"
  # Required: stable key to encrypt credentials — regenerated randomly on every restart if not set,
  # which breaks all saved credentials. Replace with a strong random secret.
  - name: N8N_ENCRYPTION_KEY
    value: "ba60890c0393e5d323309861d3a6c2ce1d8c892fe13aa83f62a576caa3c10b81"